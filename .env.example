# Example .env file - copy to .env and fill with real values (DO NOT commit secrets)

# SMTP settings (use either SMTP_SERVICE for common providers like 'gmail' OR specify host/port)
SMTP_SERVICE=gmail
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-smtp-password
SMTP_FROM=your-email@gmail.com

# Optional explicit host/port
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Recommended: set NODE_ENV=production in production
# NODE_ENV=development
